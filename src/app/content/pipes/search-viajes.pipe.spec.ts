import {SearchViajesPipe} from './search-viajes.pipe';

describe('SearchViajesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchViajesPipe();
    expect(pipe).toBeTruthy();
  });
});
